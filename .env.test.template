# 테스트 환경 변수 (개발 서버와 동일한 AWS RDS 사용)
# DEV와 TEST는 같은 데이터베이스 서버를 사용하되, 다른 데이터베이스명 사용

# AWS RDS 테스트 환경 설정 (DEV와 같은 서버)
AWS_TEST_DB_URL=DB_URL
AWS_TEST_DB_USERNAME=DB_USERNAME
AWS_TEST_DB_PASSWORD=DB_PASSWORD

# JWT 키스토어 설정
JWT_KEYSTORE_LOCATION=classpath:keys/keystore.p12
JWT_KEYSTORE_PASSWORD=changeit
JWT_KEYSTORE_ALIAS=oauth-key

# entrypoint.sh 스크립트용 키스토어 설정
KEYSTORE_PATH=/workspace/src/main/resources/keys/keystore.p12
KEY_ALIAS=oauth-key
KEYSTORE_PW=changeit
DNAME=CN=wordle-auth, OU=Test, O=Wordle, L=Seoul, S=Seoul, C=KR

# AWS 리전 설정
AWS_REGION=ap-northeast-2

# 테스트 환경 설정
SPRING_PROFILES_ACTIVE=test
SERVER_PORT=8080

# 선택적 설정
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
LOGGING_LEVEL_ROOT=INFO
